
{% extends 'base.html.twig' %}

{% block title %}AddPost{% endblock %}

{% block body %}
{% if edit %}
    <h1>Update a post</h1>
{% else %}
    <h1> Add a post </h1>
{% endif %}



{{ form_start(formAddpost) }}

    {{ form_row(formAddpost.title) }}
    {{ form_row(formAddpost.description) }}
    {{ form_row(formAddpost.image) }}
    {# {{ form_row(formAddpost.country) }} #}
    {# {{ form_row(formAddpost.country) }} #}
    {# {{ dump(formAddpost.country.vars.choices) }} #}
    {% for choices in formAddpost.country.vars.choices %}
        {% for key, choice in choices.choices %}
            {{ choice.data }} 
            {# la value dans le select devra etre choice.data #}
            {# {{ dump(choice.data) }} #}
        {% endfor %}
    {% endfor %}

{{ form_end(formAddpost) }}


{% endblock %}