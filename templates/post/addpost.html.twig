
{% extends 'base.html.twig' %}

{% block title %}AddPost{% endblock %}

{% block body %}
{% if edit %}
    <h1>Update a post</h1>
{% else %}
    <h1> Add a post </h1>
{% endif %}

{# {{form(formAddstagiaire)}} #}

{{ form_start(formAddpost) }}

    
        {{ form_row(formAddpost.title) }}
        {{ form_row(formAddpost.description) }}
        {{ form_row(formAddpost.creationDate) }}
        {{ form_row(formAddpost.imageFile) }}
        {{ form_row(formAddpost.user) }}
    
    
{{ form_end(formAddpost) }}


{% endblock %}