{% extends 'user.html.twig' %}

{% block title %}DetailUser{% endblock %}

{% block body %}
<div>
    <div  style="display: flex; flex-direction: column; align-items: center; width: 25rem;">
        <h1> {{ user }}</h1>
        <p>
            title: {{user.email}} <br>
        </p>
    </div>
    {% for post in user.posts %}
    {{post.title}}

    {% endfor %}
</div>
{% endblock %}