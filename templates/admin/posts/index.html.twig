<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <title>{% block title %}list of Posts{% endblock %}</title>
</head>
<body>

    <div class="container">
        <div class="row">
            {% include "components/_adminNav.html.twig" %}
            <div class="col-9 border border-light bg-light p-3">
                <h3>list of Posts</h3>
                <table class="table">
                    <thead>
                        <tr>
                            <th>Post id</th>
                            <th>List de postes</th>
                            <th>date de creation</th>
                            <th>Nombre de commentaires</th>
                            <th>Modifier</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for post in posts %} 
                        <tr>
                            <td>{{post.id}}</td>
                            <td>{{post.title}}</td>
                            <td>{{post.creationDate | date ('d/m/Y') }}</td>
                            <td>{{post.comments|length }}</td>
                            <td><a href="{{ path('admin_posts_edit_post', {'id': post.id})}}" class="btn btn-warning">Modifer</a></td>
                        </tr>
                        {% endfor %}
                    </tbody>

                </table>
            </div>
        </div>
    </div>
   
</body>
</html>


